#include "main.h"

/**
 * _strlen(char *s) - entry point
 * Description :  function that returns the length of a string.
 * Returns : int
 */

int _strlen(char *s)
{
	int len = strlen(s);
	return len;
}
